name: 保持活动状态
on:
  schedule:
    - cron: "0 0 * * *"

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 保持仓库活跃
        run: |
          echo "✅ Keepalive at $(date -u)"
          echo "Repository is active."
